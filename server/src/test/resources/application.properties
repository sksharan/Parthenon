# Datasource for test database
spring.datasource.url=jdbc:h2:mem:;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL;INIT=CREATE SCHEMA IF NOT EXISTS "public";
# Use flyway for initialization instead of classpath:data.sql and classpath:schema.sql
spring.datasource.initialize=false